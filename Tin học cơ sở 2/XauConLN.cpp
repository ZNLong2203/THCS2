#include<stdio.h>
#include<string.h>
int main() {
	char str[100000];
	char luu[10000];
	gets(str);
	int max1,so,j;
	int max2=0;
	for(int i=0;i+1<strlen(str);i++) {
		for(int j=i;j<strlen(str);j++) {
			
		}
	}
}
